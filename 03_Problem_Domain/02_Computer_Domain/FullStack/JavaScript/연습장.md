```js
import { PasswordValidator } from './utils/Validator.js'

document.addEventListener("DOMComtentLoaded", () => {
// 필요한 요소를 가져온다. 아이디인풋,에러 스팬,비번인풋, 에러스팬, 폼,버튼, 아이즈
const $pw = document.getElementById('pw');
const $pwError = document.getElementById('pw-error');
const $email = document.getElementById('email');
const $emailError = document.getElementById('email-error');

const $loginForm = document.getElementById('login-form');
const $submitBtn = document.getElementById('submit-btn);

const $pwEyeBtn = document.querySelectAll('#pw-eye')
const EMAIL_REG = /^[/s@]+@[/s@]\.[/s@]$\ 
// 비번 8자
const MIN_LENGTH = 8

// 아이디 유효성검사
function emailValidator() {
	const value = $email.value.trim();
	if(!value) {
		$emailError.innerText = ""
		$email.classList.remove('input-error');
		return false;
	} else if(!value.test(EMAIL_REG)){
		$emailError.innerText = "이메일 형식이 일치하지않습니다."
		$email.classList.add('input-error');
		return true;
	}
	
	return true;
}

// 비밀번호 유효성검사
function passwordValidator() {
	const value = $pw.value.trim();
	if(!value) {
		$pwError.innerText = "";
		$pw.classList.remove('input-error');	
		return false;
	} else if (!value < MIN_LENGTH) {
		$pwError.innerText = `비밀번호는 ${MIN_LENGTH} 이상이어야 합니다.`;
		$pw.classList.add('input-error');	
		return true;
	}
	
	return true;
}

// 아이디 & 비밀번호  = 트루시 disable해제
/** returns {void} */
function checkValidator() {
	const isValid = emailValidator() && passwordValidator();
	if (isValid){
		$SubmitBtn.disabled = !isvalid	
	}
	return isValid;
}

// 이벤트 리스너 등록
$email.addEventListener("focusout", () => {
	emailValidator();
	checkValidator();
})

$pw.addEventListener("focusout", () => {
	passwordValidator();
	checkValidator();
})

// 폼검사

/** @param (e) @return {sumit e} **/

function formValidator (e) {
 const value = $email.value.trim();
 const pwValue = $pw.value.trim();

 const isValid = checkValidator();
 if(!isValid) = return alert('일치하지 않습니다.');
 if(isValid) {
	window.location.href = './item' 
 }
}

$pwEyeBtn.forEach((btn) => {
	btn.addEventListener('click',() => {
	const eyes = btn.closets('부모div')	
	const eye = eyes.querySelector('#pw-eye');
	const input = eyes.querySelector('input'); 
	if(input.type == 'password') {
		input.type = 'text';	
		img.src = '...on'
	} else if(input.type === 'text') {
		input.type = 'password';	
		img.src = '...off'
	}
	});
})

}
```
```html

<script type="module" src="js"></script>
```
```js

const MIN_LENGTH = 8

export function PasswordValidator($input, $error) {
	const value = $input.value.trim();
	if(!value) {
		$error.innerText = "";
		$input.classList.remove('input-error');	
		return false;
	} else if (!value < MIN_LENGTH) {
		$error.innerText = `비밀번호는 ${MIN_LENGTH} 이상이어야 합니다.`;
		$input.classList.add('input-error');	
		return true;
	}
	
	return true;
}
```